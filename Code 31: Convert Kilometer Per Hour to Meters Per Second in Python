Here is the Python program to convert Kilometer Per Hour to Meters Per Second.
# Formula- Multiply the number by 1000 and then divide it by 3600.
def kmph_to_mps(speed):
    mps = speed * 1000/3600
    return mps
print(kmph_to_mps(100))
print(kmph_to_mps(600))
